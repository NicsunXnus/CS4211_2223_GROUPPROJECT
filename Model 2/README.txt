Each Season takes about 15min so an estimate of 1h30min is needed.
Generated csv files are in new probabilities/csv for checking.

Steps to run:
1) Edit the Main.py file with the correct directory paths
2) Go to Command Line and run Main.py. Default mode should run for all seasons.
3) After all has been ran, go into new probabilities folder and manually export each corrupted csv files into new csv files with a correct filename etc; 20152016 -> 1516
4) Run simulate.py on command line