Each Season takes about 15min so an estimate of 1h30min is needed.
Generated csv files for the new probabilities are in new_probabiltiies/done

Steps to run:
1) Edit the MainN.py file with the correct directory paths
2) Go to Command Line and run MainN.py. Default mode should run for all seasons.
3) After all has been ran, go into new probabilities folder and pls manually export each corrupted csv files into new csv files with a correct filename etc; 20152016 -> 1516
4) Run simulate.py on command line

For result 4, pls replace the 20152016 matches file with the one in the model 4 folder
For result 5/6, pls replace the MainFunctionsNN with the one in the folder. Also pls continue to use the new 20152016 matches file for result 4.